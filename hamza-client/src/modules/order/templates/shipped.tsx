import React from 'react';

const Shipped = () => {
    return (
        <div>
            {/* Shipped-specific content */}
            <h1>Shipped Orders</h1>
        </div>
    );
};

export default Shipped;
