import React from 'react';

const Cancelled = () => {
    return (
        <div>
            {/* Cancelled-specific content */}
            <h1>Cancelled Orders</h1>
        </div>
    );
};

export default Cancelled;
